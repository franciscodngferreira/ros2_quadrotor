# colcon / ROS 2 workspace
build/
install/
log/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.eggs/
dist/

# Local / runtime state under workspace
.ros/
.ros_run/
.ros_run*/
.ros_warnfix/
.gz/
.gz_run/
.gz_run*/
.gz_warnfix/

# Training / experiment outputs (regenerate or store elsewhere)
hover_tensorboard/
checkpoints/
checkpoints_goal*/
quadrotor_hover_ppo.zip
quadrotor_goal_ppo*.zip
quadrotor_goal_ppo*.pkl
train.log
train_prev_*.log

# Demo captures — the committed artifact is docs/demo.gif; the raw GUI
# recordings are tens of MB and would live in git history forever.
*.mp4

# Editors
.idea/

# OS
.DS_Store
Thumbs.db

# Personal notes (optional — remove this line if you want it in git)
diary.md
